---
dist: bionic
language: python
python:
  - 3.8
sudo: required
services:
  - docker

jobs:
  include:
    - stage: full-test
      install:
        - pip install --upgrade pip
        - make venv
      script:
        - set -e
        - make build/base-notebook
        - travis_wait 60 make build/illumidesk-notebook
        - make test

stages:
  - name: full-test
    if: branch = main
